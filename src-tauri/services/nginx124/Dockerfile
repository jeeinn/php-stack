# Dynamic base image support
# Users can override by setting NGINX124_VERSION in .env (e.g., nginx:1.24, nginx:1.25-alpine)
ARG NGINX_BASE_IMAGE=nginx:1.24-alpine
FROM ${NGINX_BASE_IMAGE}

WORKDIR /www
