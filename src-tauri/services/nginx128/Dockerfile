# Dynamic base image support
# Users can override by setting NGINX128_VERSION in .env (e.g., nginx:1.28, nginx:1.27-alpine)
ARG NGINX_BASE_IMAGE=nginx:1.28-alpine
FROM ${NGINX_BASE_IMAGE}

WORKDIR /www
